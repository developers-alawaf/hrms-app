MONGODB_URI=
JWT_SECRET=
PORT=5000
APP_TIMEZONE=Asia/Dhaka
APP_NAME=HRMS

# Email (for leave/remote notifications, password reset, invitations)
# Zoho: smtppro.zoho.com (Mail Pro) or smtp.zoho.com (standard), port 587
# Gmail: smtp.gmail.com, port 587, MUST use App Password (myaccount.google.com/apppasswords)
#        Regular Gmail password will fail with "535 Username and Password not accepted"
MAIL_HOST=smtppro.zoho.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=
MAIL_FROM_NAME=HRMS

# Optional: ZKTeco device (leave empty to run without device sync)
# ZKTECO_DEVICE_IP=
# ZKTECO_DEVICE_PORT=4370
# ZKTECO_INPORT=5200